// Make the API request
const regalo = document.getElementById('nuevo__regalo')

regalo.addEventListener('click', async () => {
  try {
    const amazonAdd = document.getElementById('cita__image')

    console.log(amazonAdd);
  }
    catch(error){
      console.error('EventListenerErrorRegalo:', error);
    }
})
